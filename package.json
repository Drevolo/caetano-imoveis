{
  "name": "caetano-imoveis",
  "version": "2.0.0",
  "description": "Site institucional e vitrine de imóveis da Caetano Imóveis",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "build:min": "node build.js --minify",
    "dev": "node build.js && npx live-server dist --port=3000 --open=index.html --quiet",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "html-minifier-terser": "^7.2.0",
    "live-server": "^1.2.2",
    "terser": "^5.31.0"
  }
}
